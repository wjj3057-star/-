<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Framework Material rather than AppCompat/Material Components: minSdk is 33, so
        there is nothing left for those libraries to back-port, and AppCompat's inflation
        interceptor would rewrite every view in the overlay for no benefit.
    -->
    <style name="Theme.ImaxCam" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="Theme.ImaxCam.Fullscreen">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="HudText">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:shadowColor">#CC000000</item>
        <item name="android:shadowRadius">3</item>
    </style>
</resources>
